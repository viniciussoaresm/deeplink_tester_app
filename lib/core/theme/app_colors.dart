import 'package:flutter/material.dart';

// Cores de referência (do código anterior)
const Color primaryColor = Color(0xFFFF8C42); // Laranja/Coral
const Color lightTextColor = Color(0xFF222222); // Texto escuro (para o tema Light)
const Color darkTextColor = Colors.white; // Texto claro (para o tema Dark)
const Color hintColorLight = Color(0xFFAAAAAA); // Texto auxiliar (Light)
const Color hintColorDark = Color(0xFFDDDDDD); // Texto auxiliar (Dark)